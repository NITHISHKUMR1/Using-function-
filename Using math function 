//using math Function at sqrt
#include <stdio.h>
#include <math.h>

int main() {
    double num,res;
    printf("enter the number");
    scanf("%lf",&num);
    res = sqrt(number);
    printf("Square root of %.2lf = %.2lf\n", num, res);
    return 0;
}