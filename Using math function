//using math Function at power 
#include <stdio.h>
#include <math.h>

int main() {
    int ab,square; 
    printf("enter the number");
    scanf("%d",&ab);
    square  = pow(ab,3);
    printf("power of value is %d=%d",ab,square);
    return 0;
}
